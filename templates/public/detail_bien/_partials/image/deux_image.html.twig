{#
 Template: templates/public/detail_bien/_partials/image/deux_image.html.twig
 Role: Renders the public/detail_bien/_partials/image Twig view.
 Context: Provided by the rendering controller, parent template, or including template.
 Maintenance: Preserve blocks, includes, macros, and data attributes used by this view.
#}

{% set pictures = property.propertyImages|default([]) %}

{% if pictures|length == 2 %}
    <div class="bt-gallery bt-gallery--count-2 row row-cols-2 g-3">

        {% for picture in pictures %}
            <div class="col">
                <button
                    class="bt-gallery__item ratio ratio-4x3 d-block w-100 border-0 p-0 bg-transparent overflow-hidden
                        {% if loop.first %} rounded-start-4{% endif %}
                        {% if loop.last %} rounded-end-4{% endif %}
                    "
                    type="button"
                    data-open-slider="{{ loop.index0 }}"
                    aria-label="Ouvrir la photo {{ loop.index }}"
                >
                    <img
                        src="{{ vich_uploader_asset(picture, 'imageFile') }}"
                        class="js-bt-img w-100 h-100 object-fit-cover"
                        alt="Photo {{ loop.index }}"
                        loading="lazy"
                    >
                </button>
            </div>
        {% endfor %}

    </div>
{% endif %}
