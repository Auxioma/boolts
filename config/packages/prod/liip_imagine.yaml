liip_imagine:
    loaders:
        biens_s3_loader:
            flysystem:
                filesystem_service: "biens.storage"

    data_loader: biens_s3_loader

    resolvers:
        s3_cache:
            flysystem:
                filesystem_service: "liip_cache.storage"
                root_url: "%env(string:N0C_PUBLIC_ROOT_URL)%"
                cache_prefix: "media/cache"
                visibility: public

    cache: s3_cache