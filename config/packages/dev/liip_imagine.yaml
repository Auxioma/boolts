liip_imagine:
    loaders:
        biens_loader:
            flysystem:
                filesystem_service: "biens.storage"

    resolvers:
        default:
            web_path:
                web_root: "%kernel.project_dir%/public"
                cache_prefix: "media/cache"

    data_loader: biens_loader
    cache: default